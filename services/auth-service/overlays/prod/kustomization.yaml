apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: auth-service-prod
nameSuffix: -prod

commonLabels:
  environment: prod

replicas:
- name: auth-service
  count: 2
